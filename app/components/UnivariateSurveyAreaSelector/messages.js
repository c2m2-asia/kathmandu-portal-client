/*
 * UnivariateSurveyAreaSelector Messages
 *
 * This contains all the text for the UnivariateSurveyAreaSelector component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.UnivariateSurveyAreaSelector';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UnivariateSurveyAreaSelector component!',
  },
});
