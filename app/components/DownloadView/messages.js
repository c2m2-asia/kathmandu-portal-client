/*
 * DownloadView Messages
 *
 * This contains all the text for the DownloadView component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DownloadView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DownloadView component!',
  },
});
