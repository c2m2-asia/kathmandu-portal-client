/*
 * ScrollToTopButton Messages
 *
 * This contains all the text for the ScrollToTopButton component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ScrollToTopButton';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ScrollToTopButton component!',
  },
});
