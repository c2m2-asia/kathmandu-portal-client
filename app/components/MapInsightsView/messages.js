/*
 * MapInsightsView Messages
 *
 * This contains all the text for the MapInsightsView component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MapInsightsView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MapInsightsView component!',
  },
});
