/*
 * MetaTable Messages
 *
 * This contains all the text for the MetaTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MetaTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MetaTable component!',
  },
});
