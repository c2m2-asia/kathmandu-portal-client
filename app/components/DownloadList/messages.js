/*
 * DownloadList Messages
 *
 * This contains all the text for the DownloadList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DownloadList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DownloadList component!',
  },
});
