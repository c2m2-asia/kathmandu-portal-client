/*
 * ControlsBar Messages
 *
 * This contains all the text for the ControlsBar component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ControlsBar';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ControlsBar component!',
  },
});
