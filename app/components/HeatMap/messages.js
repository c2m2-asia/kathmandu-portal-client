/*
 * HeatMap Messages
 *
 * This contains all the text for the HeatMap component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HeatMap';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HeatMap component!',
  },
});
