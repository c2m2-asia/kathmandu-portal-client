/*
 * SpotlightWalkthrough Messages
 *
 * This contains all the text for the SpotlightWalkthrough component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SpotlightWalkthrough';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SpotlightWalkthrough component!',
  },
});
