/*
 * DimensionTable Messages
 *
 * This contains all the text for the DimensionTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DimensionTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DimensionTable component!',
  },
});
