// import { selectMapInsightsPageDomain } from '../selectors';

describe('selectMapInsightsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
