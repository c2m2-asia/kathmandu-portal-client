// import { selectUnivariateChartPageDomain } from '../selectors';

describe('selectUnivariateChartPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
