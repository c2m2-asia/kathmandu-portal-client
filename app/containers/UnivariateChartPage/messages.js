/*
 * UnivariateChartPage Messages
 *
 * This contains all the text for the UnivariateChartPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UnivariateChartPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UnivariateChartPage container!',
  },
});
