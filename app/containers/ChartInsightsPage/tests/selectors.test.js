// import { selectChartInsightsPageDomain } from '../selectors';

describe('selectChartInsightsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
